# OpenApply Agent Rules

## GitHub PR & Merge Policy
- **DO NOT** automatically create Pull Requests or merge PRs to `main` unless the user **EXPLICITLY** requests or instructs to do so in the prompt.
- When code changes are made, keep them local or on a feature branch until explicitly instructed by the user to open a PR or merge.
